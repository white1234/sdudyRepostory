<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studydemo.demo.mapper.SysRoleMapper">
    <select id="listUserRoles" resultType="com.studydemo.demo.model.entity.SysRoleInfo">
        select role.id,role.role_name,role.role_express
        from sys_role_info role left join
        sys_user_role userRole
        on userRole.role_id = role.id
        where userRole.user_id = #{userId}
    </select>
    <insert id="saveUserRole" >
        insert into sys_user_role(id,user_id,role_id) values (#{id},#{userId},#{roleId})
    </insert>
</mapper>